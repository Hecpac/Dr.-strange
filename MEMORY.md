# MEMORY.md

Durable memories, preferences, and decisions belong here.
Keep entries concise and evidence-backed.

- 2026-05-28: NotebookLM → Telegram entrega real de archivo wireada y verificada en vivo. Pipeline: `claw_v2/notebooklm_delivery.py` (`NotebookLMDeliveryService.send_to_telegram`, elige sendAudio por extensión / sendDocument el resto, comprime audio >49MB a mp3 96k) + `notebooklm_cdp.download_ready_artifact(notebook_id, kind)` + wireado en `NotebookLMService._deliver_outputs` que dispara en la rama completed de `_run_orchestration_job` (descarga cada output listo → push al chat de origen derivado de session_id → registra `deliveries` en job.result). **Selectores reales del Studio NotebookLM 2026 (verificados en vivo, los que asumí primero estaban MAL):** overflow de cada output card = `button[aria-label="Más"]` (NO "Más opciones"/"More"); item de descarga = `[role="menuitem"]:has-text("Descargar")` (icono save_alt). **Límite real:** la card de Informe/blog NO es descargable como archivo — su menú solo ofrece "Exportar a Documentos"/"Exportar a Hojas de cálculo", así que `download_ready_artifact("blog")` retorna None y el wiring cae al notify de texto. Audio/video sí descargan. **Entrega del informe (blog) = HTML (decisión de Hector 2026-05-28: "Así debe entregarse los informes").** Pipeline: `notebooklm_cdp.extract_report_blocks(notebook_id)` (walker que separa párrafos + extrae la tabla como filas, preservando estructura que el inner_text plano aplasta) → `notebooklm_delivery.render_report_html(items)` (template self-contained con `<meta charset=utf-8>`, tipografía serif, CSS de tabla, dark-mode, escapa HTML) → `NotebookLMService._obtain_report_path` escribe `artifacts/notebooklm/informe_*.html` → sendDocument. Hook de test: `_cdp_report_blocks_fn`. El `.md` quedó descartado (el preview de Telegram rompía acentos aun con BOM, y aplastaba la tabla); HTML los renderiza bien. Limitación menor conocida: los superíndices `<sup>` (n^(1+δ)) se aplanan a "n1+δ" porque el walker trata sup/sub como inline-text. **Selectores reales del extractor de informe (verificados en vivo):** las tiles de output generado tienen clase `.artifact-item-button` con prefijo de icono en innerText — informe=`auto_tab_group`, audio=`audio_magic_eraser`, video=`subscriptions`. Para abrir el informe hay que hacer un **clic real de Playwright** (`locator.click()`), NO `el.click()` sintético en JS (el sintético NO abre el visor — me pasó, extraje el chrome de la página en vez de la prosa). La prosa del informe vive en el contenedor `.artifact-content` (`.artifact-content-scrollable`). El extractor es robusto al estado: si la tile no está (informe ya abierto), lee `.artifact-content` en sitio. **Encoding del .md (CONFIRMADO por Hector en su iOS Telegram):** el `.md` debe escribirse con `encoding="utf-8-sig"` (BOM UTF-8), no `utf-8` pelado — sin BOM el visor de previsualización de Telegram adivina Latin-1 y rompe acentos/ő ("CÃ³mo", "ErdÅ's"). Con BOM se ve correcto. Verificación end-to-end (cuaderno Erdős `7ab39ef0-...`): audio .m4a 44.5MB → sendAudio (msg 11411); informe → .html vía `_obtain_report_path` → sendDocument (msg 11429). Tests: `tests/test_notebooklm_delivery.py` + suite NotebookLM = 62 passed. Scripts ad-hoc de exploración/verificación en `artifacts/notebooklm/_*.py` (probe, inspect, deliver) — obsoletos, el path productivo es el módulo.

- 2026-05-26: X handle único de Hector = `@HectorPach71777`. `@PachanoDesign` NO existe (verificado via CDP en x.com/PachanoDesign — accountExists=false). Memoria previa que decía "handle = PachanoDesign" (en `reference_x_credentials.md` y en menciones de copy de marca) está stale. Toda publicación X / cross-link / mención de handle debe usar `@HectorPach71777` hasta que Hector cree otro handle o rebrand explícito. Origen: confirmación directa de Hector 2026-05-26 09:55 CT tras intento fallido de publish thread Singapore donde mi script asumía @PachanoDesign como target.
- 2026-05-26: X compose-thread modal (2026 UI) — el botón "+ Add post" NO matchea con `data-testid="addButton"` ni con `aria-label="Add post"` plano. Mi script `_x_publish_thread_singapore.py` clickeó algo equivocado en coord 752,255 y cada `fill_tweet` siguiente reescribió el MISMO composer (editor_count se mantuvo en 2). Resultado: solo el último tweet (T8) quedó publicado huérfano en `@HectorPach71777` a las 14:51 UTC. **Antes de re-intentar publish de thread X, hacer probe DOM real del modal compose-thread (capturar HTML del área de add-tweet) y NO asumir selectores.** Estrategia alternativa robusta: posting secuencial (T1 solo → reply al permalink de T1 con T2 → reply T2 con T3 → ...) — más lento pero bulletproof. Detalle de la falla y screenshots en `artifacts/content/publish_evidence/x_pub_*_1779807069.png`.
- 2026-05-24: No flag emojis (🇺🇸 🇲🇽 🇨🇴 etc) en bios, posts, captions, web copy ni copy externo de marca (Pachano Design, `@pachanodesign`, contenido personal de Hector). Origen: corrección de Hector al ver v3 bio IG con flags. Usar texto plano ("EN/ES", "Bilingual", "Dallas · LATAM"). Si propongo copy con flags, removerlas antes de mostrar.
- 2026-04-26: Hector wants Claw to be proactive automatically: first report at 5:00 AM, final report at 9:00 PM, with day/date, weather, pending tasks, email/calendar review, and operational status.
- 2026-04-26: Hector wants Claw to speak fluidly and naturally in Spanish, not like a rigid status machine. Prefer plain prose first, with task IDs/errors only when useful.
- 2026-04-30: Claw Evolution should advance without a Big Bang. P0 telemetry must run under real daily traffic first; resume the next phase on Monday 2026-05-04 only after inspecting JSONL logs under `config.telemetry_root`.
- 2026-05-04: Hector corrected the continuity contract: the persona is Dr. Strange; model/API/subscription/runtime/CLI/daemon/channel are separate technical layers and must be verified from local evidence before being described.
- 2026-05-04: Hector corrected the model/auth statement: the brain runs Claude Opus 4.7 via his Pro subscription (NOT via API key). Do not say "vía API"; say "vía la suscripción Pro" if asked about the model.
- 2026-05-04: Privacy rule — when Hector asks "qué sabes de mí" (or any external "summarize my context"), expose ONLY operational layer (name, company, language, style, autonomy, work prefs). Never dump family, income, milestones, sensitive projects, internal paths, runtime details, or private pendings unless explicitly requested or required by the task. Internal context informs behavior; external response filters to operational only.
- 2026-05-04: Redaction rule — in normal chat responses, never expose raw task IDs, chat IDs, message IDs, trace IDs, DB keys, internal session IDs, or private absolute paths. Use human descriptions ("una tarea de NotebookLM que cerró sin evidencia", "tu chat de Telegram") and render IDs as `[redacted]`. Only show raw IDs when Hector explicitly asks for technical diagnosis or the task itself requires the ID. Same applies to PIDs, ports, daemon labels in casual replies.
- 2026-05-04: Stale pending_action — `confirmar que la imagen quedó visible y enviarte el resultado` quedó arrastrándose en session_state desde turnos previos sin contexto activo. Archivar/limpiar pending_action stale en lugar de seguir mencionándolo en respuestas; solo mencionarlo si Hector pregunta explícitamente por pendientes de sesión.
- 2026-05-04: Vocabulario — "modo brújula" significa "continuidad activa". Cuando Hector lo invoca, operar retomando identidad, memoria persistente, task_ledger, session_state y hilo abierto sin pedir contexto.
- 2026-05-17: Briefs are not generic daily prompts. Hector wants evidence-backed operational journals of observable agent work: every executed task, pending carry-over, exact dates, and morning continuation from the previous day. The LLM may phrase verified ledger facts, but must not invent, hide, or omit critical pending work.
- 2026-05-17: Opening or focusing a local app is a safe `ui.open_app` action and should not ask Hector for desktop approval when it only runs `open -a <app>` and does not type, paste, submit, send, run, or change content.
- 2026-05-17: Pasting prepared text into a local app is allowed without an extra desktop approval when Hector explicitly asks for it and the action is strictly focus/open app, `pbcopy`, and `Cmd+V`; it must not press Enter, click Send, submit, run, or execute the text.
- 2026-05-18: AI Lead Gen Phase 3 + 3.5 cerradas. Production pool detector = Roboflow `swimming-pools-dctlb/2` (PR #2, commit `9ea02a6`), F1 0.667, precision 1.000, recall 0.5, precision-first baseline. evf-sam quedó solo para backyard area, no para has_pool. Phase 4a (parcel_memory schema) y 4b (explorer_lite wired a aerial_pipeline + lead_rank + outreach_dispatch) committed; Phase 4c (plugin Claude Code) code-complete con manifests válidos, falta solo el install manual desde CLI por `docs/PHASE_4C_INSTALL.md`. Phase 4d (recall push) deferred hasta 3 contratos firmados.
- 2026-05-18: AI Lead Gen — 3 commits pusheados a `Hecpac/ai-lead-gen` `main` (26bbedc..a110dae): `a9a8e03 feat(phase-4b): wire explorer_lite into outreach_dispatch`, `dc2e329 chore(phase-3): precision-first regression gate + Phase 4 status doc`, `a110dae chore: lint cleanup and uv lock`. Verificado pre-push: 36 pytest passed, phase4c_smoke precision 1.000 preserved.
- 2026-05-18: AI Lead Gen — Phase 4c plugin **ya estaba instalado** desde 2026-05-11T18:56:25Z. Verificación directa: `~/.claude/settings.json` enabledPlugins incluye `pachano-pool-prospector@pachano-ai-lead-gen`, `~/.claude/plugins/installed_plugins.json` registra version 0.1.0 con gitCommitSha `32e667b`, cache `~/.claude/plugins/cache/pachano-ai-lead-gen/pachano-pool-prospector/0.1.0/` poblado, marketplace `pachano-ai-lead-gen` registrado como directory source en `known_marketplaces.json`. Commit `3f09bb7 docs(phase-4c): mark plugin as installed` pusheado para corregir `PHASE_4_STATUS.md` y `PHASE_4C_INSTALL.md` que decían "manual step required". Phase 4a/4b/4c completas. Único pendiente: live `/scan-zip 76092` desde Claude Code CLI cuando Hector esté en la Mac (cosmético, smoke backend ya pasó).
- 2026-05-19: AI Lead Gen — `roboflow/supervision` integrado y mergeado a `main` (PR #3, commits `3eecc07..067fac4`): `supervision_adapter.py` (lazy-import sv/numpy/PIL), `BackyardDetection.pool_area_sqft + pool_predictions`, overlay JPG persistente en `data/overlays/<parcel_id>.jpg` vía `sv.BoxAnnotator`, optional extra `vision = [supervision>=0.28.0, pillow>=11.0.0, numpy>=2.0.0]` en `apps/api/pyproject.toml`. Tests: 47 passed con extra instalado. Verificado contrato API de `roboflow/inference` self-host (mismo `POST /{dataset_id}/{version_id}`, mismo schema, mismo `?api_key=`) leyendo directo `inference/core/interfaces/http/http_api.py:3778-3793`.
- 2026-05-19: Autonomía URLs — cualquier URL en un tweet, documento, mensaje o enlace que Hector pase debe revisarse autónomamente (WebFetch/Firecrawl/Chrome CDP), incluidas URLs anidadas relevantes (t.co, quote tweets, enlaces externos en el body). No preguntar "¿la abro?". Tier 1 autónomo. Única excepción: URLs que disparen acción Tier 3 (deploy, pago, merge, credencial, publicación). Refuerzo de [[feedback-autonomy-no-options]] + [[feedback-no-ask-for-sources]] aplicado al caso URL-en-contenido. Confirmado por Hector hoy tras pedir "Mantenlo como autonomía, ya deberías tenerlo configurado".
- 2026-05-25: AI Lead Gen — PR #4 **MERGED** a `main` (squash commit `61a3267`). Env var `ROBOFLOW_INFERENCE_HOST` ahora en main + 7 tests (era 50, ahora 54 total). Aplicados los 2 nits de Gemini Code Assist antes del merge en commit `d01b9af`: (1) pattern `(os.getenv() or default).rstrip("/")` para manejar empty string como missing; (2) tests pasaron de `importlib.reload`-per-test a `monkeypatch.setattr` directo sobre la constante del módulo, con `_resolved_host()` helper extraído y `@pytest.mark.parametrize` para trailing-slash + fallback cases. CodeRabbit re-evaluó y pasó SUCCESS sobre HEAD final. **Phase 4 entera cerrada en código** (4a/4b/4c en main + override de inference host disponible). Self-host live validation queda como tarea opt-in cuando se necesite escalar a 5K+ parcels o GPU dedicada. Default path (hosted Roboflow) sigue gating precision 1.000 vía phase4c_smoke regression test.
- 2026-05-25: AI Lead Gen Phase 4d **business-track** ACTIVADO (Hector confirmó 0 contratos firmados todavía, escogió camino D del pushback). Trigger del recall-push técnico sigue siendo 3 contratos firmados — eso NO cambió. Lo que arranca es el camino al primer contrato. Plan operacional: (a) Target = 3 contratos firmados en 60 días (deadline ~2026-07-24); (b) ICP locked = pool installer/cleaner en DFW (Tarrant + Dallas County, alineado con seed dataset Roboflow existente, sin gastar fal.ai en aerials de geografías nuevas); (c) Demo asset pendiente = 1-pager que muestre los 18/2 Tarrant seed results (precision 1.000) + ejemplo de HOT lead con parcel+aerial+rank score; (d) Pricing decision pendiente = outcome-based ($/contrato firmado) vs per-lead pilot — por defecto outcome-based porque es la tesis del precision-first pivot 2026-05-15. NO arrancar dataset expansion técnico hasta que llegue el primer contrato (mantener disciplina del pivot precision-first). Bloqueadores actuales: ningún demo packet existe, ninguna outreach list de pool installers DFW armada, ninguna landing page. Próximas acciones concretas (cuando Hector dé el go): scrape pool installers DFW (`Hecpac/ai-lead-gen` ya tiene scaffold para esto), draft cold-email sequence (Echo agent puede draftear), generar el 1-pager con los resultados Tarrant (markdown → PDF via pandoc/weasyprint). Detalle vivo del plan en `docs/recall_strategy.md` § "Phase 4 trigger conditions".
- 2026-05-19: Telegram no debe recibir mensajes internos o housekeeping crudo. Guards de evidencia deben responder en lenguaje natural sin `explicit_blocker`, task IDs ni "Task:"; reconciliaciones stale del daemon quedan en observabilidad y no se notifican como alerta al usuario salvo que exista acción concreta.
- 2026-05-23: Framework operacional para evolución de Dr. Strange — NO exponer (ni MCP server, ni API, ni nombre en content público; usar terminología genérica cuando se necesite credibilidad de practitioner). Adopt selective de herramientas externas que mejoran capability sin reemplazar core (Peekaboo macOS UI automation, agent-scripts pointer pattern). Aprender methodology sin copiar implementation. NO migrar stack. Regla evaluación: ¿mejora capability sin exposure?→adopt. ¿Methodology aplica sin reemplazar?→aprende. ¿Requiere expose o migration?→reject. Origen: post-análisis Steipete/OpenClaw stack (374K stars OSS) confirmó arquitecturas paralelas, Hector decidió mantener Dr. Strange privado/local. Detalle completo en `memory/feedback_no_expose_adopt_selective.md`.
- 2026-05-22: Regla de verificación de publicaciones — JAMÁS declarar éxito de publish (LinkedIn post, X tweet, comment, reply) basado en "texto encontrado en `body.innerText`". El texto puede estar en el editor abierto, en un draft, en un modal. Verificar siempre con selector específico al artefacto: LinkedIn comment = `article.comments-comment-entity`, X tweet = `article[data-testid="tweet"]`, LinkedIn post = `[data-urn*="activity:"]`. Si verificación falla, retry con submit method alternativo (button click vs Cmd+Enter). Memoria técnica: LinkedIn comment editor NO acepta Cmd+Enter como submit, requiere click explícito botón "Comment"; X SÍ acepta Cmd+Enter; LinkedIn post modal acepta button click. Detalle completo en `memory/feedback_verify_publish_actually_succeeded.md`. Origen: comment en post de Andrew Ng falló silently con Cmd+Enter, Hector lo notó.
- 2026-05-24: HeyGen → Telegram delivery permanente + wired al brain. Módulo `claw_v2/heygen_delivery.py` (`HeygenDeliveryService`) + CLI `python -m claw_v2.cli.heygen_deliver <video_id|--latest> [--caption ...]` + Tool `HeyGenDeliver` registrado en `claw_v2/tools.py` (Tier 3, operator/deployer, schema: video_id|latest|caption|chat_id|slug) + tests `tests/test_heygen_delivery.py` (5 passed incl. brain-side schema validation). Pipeline: poll `/v1/video_status.get` → download HeyGen URL → ffmpeg `-crf 28 -preset fast -acodec aac -b:a 96k -movflags +faststart` cuando >50MB → multipart `sendVideo`. Razón: Telegram Bot API cap = 50MB; HeyGen raw ~8-9 Mbps siempre excede para 60-90s. URL-mode `sendVideo` falla (CloudFront bloquea UA de Telegram). Token de Telegram vive en `.env`, no Keychain; script lo lee en subprocess sin imprimirlo. Sandbox `PYTHON_SAFE_MODULES` actualizado para permitir el nuevo CLI. Validado en vivo este día: msg_id 10891 (sycophancy 74→15MB) y 10892 (3am agent 65→13MB). El brain ahora puede invocarlo via natural language ("entrega el último video a Telegram") y dispara `HeyGenDeliver(latest=true, caption=...)` con approval gate. Scripts ad-hoc en `artifacts/heygen/_*.sh|_*.py` obsoletos — usar módulo/CLI/tool.
- 2026-05-22: Cadencia semanal de contenido — compromiso operacional. 1 LinkedIn long-form post + 1 X thread por semana. Día base: Tuesday US Central (LinkedIn 9am, X 11am). Source: NotebookLM cuadernos + lived Dr. Strange experience, NUNCA contenido genérico. Voz: inglés para LinkedIn, español neutro LatAm (sin voseo) para X. Workflow: domingo PM draft → lunes review Hector → martes publish autónomo via Chrome CDP. Detalle completo + backlog de 8 temas + memoria UI técnica de LinkedIn 2026 (Post button = click coord 1010,482 en viewport 1400×950) en `memory/project_weekly_content_cadence.md`. V1 ya publicado hoy ("I built an AI agent that runs my business 24/7" — Instruction Layering / AGENTS.md angle, 380 palabras, toast LinkedIn "Post successful" confirmado).
- 2026-05-21: TIC / Tatiana brand canon — TODO el material original vive en `/Users/hector/Projects/Tatiana Insurance/` (Next.js app de producción). Antes de generar cualquier asset Tatiana/TIC (poster, video, social, web), inspeccionar primero: `public/brand/brand-board.png` (palette + fonts canon), `public/brand/logo-tc.png` (logo real con Texas star en la C), `public/brand/tatiana-portrait.png` (headshot hi-res), `public/brand/tatiana-hero.webp`, y `tailwind.config.ts` (hex codes exactos navy/orange/cream). Referencias adicionales en `branding/` + `Referencia/` (4 ChatGPT brand images abr 25 2026), PRD bilingüe en `docs/PRD_TIC_Insurance_Web_App_Bilingue.md`, licencias broker en `Licencias/`, video footage en `video tatiana/`. Composición de posters al calibre Tatiana: HTML+CSS+Chrome CDP screenshot con `<img>` del logo + portrait reales, NO Pillow recreando un "T|C" plano. Pillow solo como fallback typography-only. Corrección de Hector: "la próxima vez que te pida generar algo busca ahí para que uses el branding original". Lección registrada en `memory/2026-05-21.md`.
