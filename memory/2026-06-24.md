# 2026-06-24

## Next Session Handoff — F0-F6 / F2.2 Live Disabled

- Current deployed `main`: `5edd53d583e78dbda45186e9235833450bb97dfa`.
- Deployment verification: daemon restarted only via `bash scripts/restart.sh`; latest `agent_startup_context` was event `271951`, timestamp `2026-06-24 03:23:58`, pid `89862`, `code_version=5edd53d`.
- Watchdog smoke: `scripts/audit/watchdog_stale_filter_smoke.py --db data/claw.db --expected-code-version 5edd53d --json` returned `PASS` / `safe_candidate`, read-only, watchdog not reloaded.
- F2 production state: F2 flags were not enabled; env and `$HOME/.claw/env` had no F2 entries. Production F2 tables were absent, so no F2 migration or production writes occurred.
- F0/F1 status: live baseline. RuntimeDb/WAL/SQLite/database-locked post-boot scan after event `271951` was clean.
- F2 status: F2.0 schema, F2.1 RuntimeDb store, and F2.2 Coordinator phase checkpoint hooks are merged/deployed but disabled/no-op in production.
- F2 remaining: F2.3 external effect records, F2.4 recovery/resume, and F2.5 observability/diagnostics are not done. Do not enable F2 until those gates are implemented and verified.
- F3-F6 status for this roadmap: deferred. F3 = later lease/watchdog semantics, F4 = later forced-action gate, F5 = later browser/runtime side-effect work, F6 = later dynamic fanout/graph scheduling.

## Guardrails For The Next Session

- Do not enable `CLAW_F2_DURABILITY_ENABLED`, `F2_DURABILITY_ENABLED`, or any F2 flag unless Hector explicitly asks for an enablement run with migration gates.
- Do not manually create/migrate production F2 tables.
- Do not touch prod DBs except read-only inspection unless explicitly authorized.
- Do not reload watchdog or run browser/CDP smoke unless explicitly requested.
- If continuing F2, the next real implementation lane is F2.3 external-effect records or F2.4 recovery/resume; keep F2.2 production behavior disabled by default.
