# 2026-05-19

## AI Lead Gen — supervision integration + roboflow inference host override

### PR #3 (`roboflow/supervision` integrado) — MERGEADO

4 commits en `main` (Hecpac/ai-lead-gen) rebase-merged:
- `3eecc07` feat(aerial): supervision adapter for Roboflow output — `apps/api/services/supervision_adapter.py` con `roboflow_to_detections`, `detections_box_area_sqft`, `annotate_overlay_bytes`; supervision/numpy/PIL lazy-importadas. SQFT_PER_PIXEL_Z19 = 0.2175 sqft/px (Esri zoom 19 DFW lat ~32.7N).
- `54f6de2` feat(aerial): expose pool_area_sqft from Roboflow via supervision adapter — split `_roboflow_has_pool` en `_roboflow_pool_predictions` (raw list) + wrapper has_pool. `BackyardDetection` gana `pool_area_sqft: float = 0.0` y `pool_predictions: list[dict] = []`. confianza del detector ahora viene del max de las predicciones.
- `ba6f90f` feat(aerial): persist annotated overlays via supervision — `detect_for_parcel` escribe `data/overlays/<parcel_id>.jpg` con `sv.BoxAnnotator` cuando hay `pool_predictions`. `AerialDetectResult.overlay_path: str | None` agregado. `OVERLAY_DIR` env var + kwarg `overlay_dir=`. `.gitignore` excluye `data/overlays/`.
- `067fac4` chore(api): declare optional `vision` extra (supervision, pillow, numpy) — `[project.optional-dependencies].vision` en `apps/api/pyproject.toml`. Install via `uv sync --extra vision` o `pip install -e ".[vision]"`. uv.lock NO regenerado en este commit porque `uv` está bloqueado en sandbox — refrescar manual con `uv lock` desde `apps/api/` cuando se toque lockfile.

Tests con extra `vision` instalado: 47 passed / 0 skipped. Sin el extra: 38 passed / 9 skipped (graceful).

phase4c_smoke contra hosted Roboflow: precision 1.000 preservada, recall 0.5 baseline matched. TAR-019 TP conf 1.000, TAR-018 known FN, TAR-000 TN.

CodeRabbit review (free plan): solo walkthrough/resumen, no line-by-line, status SUCCESS. Gemini Code Assist dejó 2 nits medium priority (vectorizar `detections_box_area_sqft` con numpy slicing, deduplicar cálculo de `max_conf`). No bloqueadores — mergeamos sin aplicarlos. Si se quiere polish después, son cambios chiquitos.

### PR #4 (`ROBOFLOW_INFERENCE_HOST` env var) — ABIERTO

Branch `feat/roboflow-inference-host-override`, commit `6050a24`:
- `apps/api/services/aerial_detect.py`: nueva constante `ROBOFLOW_INFERENCE_HOST = os.getenv(..., "https://detect.roboflow.com").rstrip("/")`. `_roboflow_pool_predictions` POSTea a `f"{HOST}/{MODEL}"` en lugar de hardcodear `detect.roboflow.com`.
- `apps/api/tests/test_inference_host.py`: 3 tests (default host, override redirige POST a `http://localhost:9001/{model}`, trailing-slash normalization). Usa `importlib.reload(aerial_detect)` para re-leer env var por test.

Total tests: 50 passed (47 anteriores + 3 nuevos).
phase4c_smoke con default: precision 1.000 preservada.

Verificación previa al diseño: leí `roboflow/inference` source en `inference/core/interfaces/http/http_api.py:3778-3793` y confirmé que el server self-host expone exactamente `POST /{dataset_id}/{version_id}` con los mismos query params (`api_key`, `confidence`, `image_type`, body base64) y mismo response model (`ObjectDetectionInferenceResponse`). El `supervision_adapter` y todo downstream funcionan sin cambio adicional.

### Pendiente operativo — seguimiento

**Cuando Hector esté frente a la Mac:**
1. Levantar el self-host:
   ```
   pip install inference-cli
   inference server start --dev   # uvicorn en :9001
   ```
   (Docker CPU image ~3GB, descarga del modelo `swimming-pools-dctlb/2` al primer hit.)
2. `export ROBOFLOW_INFERENCE_HOST=http://localhost:9001`
3. `python scripts/phase4c_smoke.py`
4. **Verificación esperada:** mismo TP/FN/TN que el hosted (TAR-019 TP, TAR-018 FN, TAR-000 TN). Confianza puede variar 1-2% por diferencias de preprocessing.
5. Si pasa: mergear PR #4. El override queda dormido (env var sin setear = comportamiento actual) y disponible para batch real cuando se escale a 5K+ parcels o se mueva a GPU dedicada (RunPod, Lambda).
6. Si no pasa: investigar diferencias de preprocessing (auto-orient, contrast, grayscale, static crop — todos los `disable_preproc_*` flags que expone el server).

**Bloqueador para auto-ejecución:** docker pull ~3GB no puede correr desatendido desde sandbox; carga inicial del modelo necesita conectividad a Roboflow (mismo `ROBOFLOW_API_KEY`).

**Cuando llegue:** retomar este pendiente sin pedir contexto. Está en MEMORY.md también.

### Otros pendientes documentados aquí

- `uv lock` desde `apps/api/` cuando se toque el lockfile (~15s). Phase 4c plugin install ya marcado completado el 2026-05-18.
- 2 nits de Gemini Code Assist en PR #3 (ya mergeado): vectorización numpy + dedupe de `max_conf`. Polish opcional, no bloqueador.

### Repos candidatos evaluados hoy (de la conversación)

Top 4 que Hector pidió considerar para integraciones futuras post-supervision:
1. `roboflow/inference` ✅ ya en PR #4.
2. `pixeltable/pixeltable` — tabla multimodal declarativa para parcel × tile × detection × overlay × outreach.
3. `gradio-app/gradio` — audit board interno para revisar overlays y override manual de has_pool.
4. `PrefectHQ/prefect` — orquestación batch con retries/observabilidad cuando el loop sync rompa.

Si después de mergear PR #4 quieres seguir, mi orden de impacto sería: gradio (resuelve weekly review ya) → prefect (cuando se escale batch) → pixeltable (cuando el storage spaghetti pese).
