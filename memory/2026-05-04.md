# 2026-05-04

## Continuity boot audit

- Hector asked to audit and fix continuity, identity, memory, and boot so Dr. Strange does not start from zero each day/session.
- Verified production launchd label is `com.pachano.claw`, launcher is `ops/claw-launcher.sh`, entrypoint is `.venv/bin/python -m claw_v2.main`, cwd is `/Users/hector/Projects/Dr.-strange`.
- Fix direction: make `AgentWorkspace.startup_context()` the shared boot context for daemon/web/Telegram runtime, with `BOOT_PROTOCOL.md`, identity, user profile, persistent memory, daily notes, session state, lessons, task_ledger, and verified operational config.

## AI Lead Gen — Fase 3 avanzada hasta seed dataset labeling (RETOMA 2026-05-05)

5 commits pusheados a `Hecpac/ai-lead-gen` `main`:
1. `919ffef` heuristic scoring + synth aerial detection (factor weights = 1.00).
2. `7a786bd` fal.ai detector wired (Florence-2 inicial).
3. `c264b82` pivot a `fal-ai/evf-sam` (Florence-2 ya no existe en fal).
4. `a13d014` wire `aerial_tiles` (Esri zoom 19 + Mapbox fallback) → `fal_storage` → `aerial_pipeline.detect_for_parcel()`.
5. `e53b686` + `9b6bcff` seed dataset builder + offline labeler con 10 centroides Tarrant curados.

**fal.ai:** key recargada hoy por Hector, guardada en `~/Projects/ai-lead-gen/.env` (gitignored, servicio `FAL_API_KEY`). Endpoints reales en fal verificados: `evf-sam`, `sam2/image`, `sam2/auto-segment`, `birefnet`. NO existen Florence-2, Grounding-DINO, YOLO-World, OWL-V2 (motivo del pivot del commit 3).

**Smoke real end-to-end verificado:** Aerial Esri → fal storage → evf-sam → mask decode → sqft. `source="fal:evf-sam"` en muestras Tarrant. Hallazgo: threshold pool default (0.005) produce 19/20 falsos positivos — evf-sam over-segmenta, requiere calibración con ground-truth.

**Donde quedó al cerrar:**
- 20 parcelas Tarrant generadas en `data/seed/` (gitignored): `manifest.json`, `aerials/`, `masks/`, `overlays/`, `label.html` con manifest inlined.
- 10 centroides residenciales: Southlake, Colleyville, Keller, Trophy Club, NRH, Mansfield, Saginaw, Hurst-Bedford, Arlington (Dalworthington), Euless.
- Hector abrió `file:///Users/hector/Projects/ai-lead-gen/data/seed/label.html` en Chrome (CDP 9250) pero **NO terminó de etiquetar**.

**Próximo paso al retomar 2026-05-05:**
1. Hector termina el labeling en Chrome → click "Export labels.json" → mover el archivo a `data/seed/labels.json`.
2. Crear `scripts/threshold_sweep.py` (sweep 0.001..0.50 sobre `pool_coverage` del manifest, reportar precision/recall/F1 por threshold).
3. Aplicar threshold óptimo como nuevo `FAL_POOL_MASK_THRESHOLD` default en `apps/api/services/aerial_detect.py` y commit.
4. Si F1 > 0.7 → Fase 3 cerrada → pasar a Fase 4 (render Before/After con FLUX vía fal). Si no → ampliar dataset a 50 parcelas o cambiar a sam2 con bbox prompts.
