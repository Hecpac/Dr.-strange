# 2026-05-17

- Telegram continuity bug verified and patched: `Continua/Procede/Sigue` was intercepted by the imperative UI router as `task.continue_active_mission` before stateful continuity, causing `missing_target` clarification instead of resuming `pending_action`, `task_queue`, `reply_context`, or recent assistant proposals. Fix routes that intent through stateful follow-up first and recognizes contextual proposal questions such as "¿Sigo con #4 ... o ...?".
- Follow-up continuity bug: reply context was loaded, but declarative markdown lines like `**Pendiente:** validacion ... — requiere un "Procede"/"Continua"` were not extracted as pending actions, so `Continua` returned "¿Qué acción concreta...?". Fix normalizes markdown/bullet pending lines and strips the "requiere un..." approval instruction before resuming.
- Follow-up verified session issue: after markdown pending extraction, `Continua` resolved to `brain_shortcut`, but still went through conversational brain fallback; the brain said "arrancando" without tool/task evidence and the evidence gate blocked it. Fix routes Telegram stateful continuations with resolved objectives to the actionable task router, and records new brain tool-use ledger rows with the human source objective instead of generic `brain fallback tool-use turn`.
- Follow-up failure summary issue: "Haz un resumen de los fallos que haz tenido hoy" produced a useful brain answer, but the evidence gate replaced it with the defensive "No digo `arrancando`..." template because the report request looked like an operator action and the answer mentioned a conditional fix. Added deterministic `operational_failure_summary` routing for text and image captions backed by `observe_stream`, recent messages, and `agent_tasks`, while preserving the specific "¿Por que fallo la tarea?" diagnostic route that returns `/task_resume`.
- Follow-up task-status issue: "Estatus de las tareas" fell through every deterministic route, hit the brain, and the brain ended with "¿Voy ahora con eso...?", which `assistant_proposal_question` persisted as a malformed `pending_action`. Added deterministic task-status overview matching for `estado/estatus/status de las tareas|ledger`, bypassing brain synthesis for that case, and prevented ledger status recaps from becoming pending actions. Repaired live `tg-574707975` session_state by clearing the malformed pending action and removing the `ops:assistant:voy-ahora...` queue entry.
