# 2026-06-21

## F1.2/F1.3 D24 observation passed

- 2026-06-21 18:53:17 UTC: D24 post-F1.2/F1.3 observation checkpoint ran read-only.
- Exact F1.2/F1.3 startup event: `2026-06-20 18:24:40 UTC`, `agent_startup_context`, code_version `af1a2c9`, pid `69476`.
- Elapsed at checkpoint: about `24.477h`; D24 threshold satisfied.
- Daemon remained stable on pid `69476`, boot_id `3d3f4cb6f082b54e`, launchd runs `57`, PPID `1`.
- Production checkout remained on `af1a2c954bf17b4b298ab0bc7f2b957a5f02567c`; `origin/main` matched.
- `AGENTS.md` / `CLAUDE.md` had no drift; `.harness/` and `scripts/harness/` were absent.
- Active hooks/settings had no active `scripts/harness`, `agent_hook.py`, `pytest tests/`, or `python -m pytest tests/` commands.
- Watchdog remained unloaded.
- Diagnostics were healthy with heartbeat source `liveness_sink`, `heartbeat_stale=false`, and `web_transport_serving=true`.
- `PRAGMA quick_check` and `PRAGMA integrity_check` were `ok`.
- Post-deploy observe/spill/log evidence: `database is locked=0`, RuntimeDb/WAL/SQLite error count `0`, WAL-heal/degraded DB count `0`, daemon lifecycle traceback/error count `0`, post-deploy startup/restart event count `0`.
- RuntimeDb WAL-heal retirement remained live: RuntimeDb production path has no WAL-heal registration; RuntimeDb-backed stores keep legacy heal only in `runtime_db=None` backcompat branches.
- SQLite synchronous remained `FULL`; no `synchronous=NORMAL` carve-out was present.
- Observe maintenance VACUUM dedicated connection remained allowlisted.
- `property_graph` remained dormant in production main path.
- `buddy.db` remained out of scope; no manual D24 check opened or mutated it.
- Application-layer checks found no structured `llm_error`, `llm_timeout`, `browser_executor_failed`, `sdk_post_tool_use_failure`, `action_failed`, `job_failed`, or `autonomous_task_failed` events; post-deploy jobs were completed; no post-deploy agent task terminal failures were created/updated.
- No detected evidence of full-suite execution since deploy; no authoritative command audit was available without elevated access.
- Result: D24 passed. F1.4 watchdog stale-event filter is now eligible for separate authorization, but was not started. Watchdog remains unloaded until F1.4 authorization. Full suite against live production remains prohibited unless separately mitigated/isolated.

## PR #125 F1.4 merged, not deployed

- 2026-06-22 00:50:02 UTC: PR #125 "F1.4 Watchdog stale-event filter" was merged.
- Merge commit / new remote `origin/main`: `35759a6a356e8c74fed66f0ad05f74d81d21ef2f`.
- Merged PR head: `8abd2bf334157c8e58293a76dd6de54e67c8d5a7`.
- Prior implementation head before adversarial hardening: `fa17600d79ab79605a7ceaaef4d8816be1ae9f2a`.
- Adversarial review found and fixed a real F1.4 gap: raw string timestamp comparison could misclassify malformed or future-skewed observe timestamps. The merged hardening parses observe/startup timestamps as UTC-aware datetimes and classifies malformed/future-skewed event or boot timestamps as `unknown_relevance_errors` / unknown current-window evidence, not actionable current errors.
- Final PR #125 scope remained limited to:
  - `claw_v2/diagnostics.py`
  - `tests/test_diagnostics.py`
  - `tests/test_computer_diagnostics.py`
- No launchd plist/script changes, watchdog load/unload script changes, RuntimeDb changes, WAL-heal changes, SQLite synchronous changes, browser/F5 work, F2/F3/F4/F6 work, buddy.db changes, AGENTS.md/CLAUDE.md changes, hooks/settings changes, or harness files were included.
- Verification recorded before merge:
  - `uv run --frozen python -m unittest tests.test_diagnostics tests.test_computer_diagnostics -v`: 45 tests passed.
  - Targeted diagnostics/watchdog/liveness/observe/computer/chrome unittest set: 112 tests passed.
  - `uv run --frozen python -m unittest tests.test_runtimedb_wiring -v`: 19 tests passed.
  - `uv tool run ruff check claw_v2 tests`: passed.
  - `uv tool run ruff format --check claw_v2 tests`: passed.
  - `git diff --check`: passed.
- Merge-only operation did not deploy F1.4. Production checkout was not fast-forwarded.
- Post-merge production read-only check at `2026-06-22 00:50:42 UTC`:
  - production HEAD remained `af1a2c954bf17b4b298ab0bc7f2b957a5f02567c` (`af1a2c9`);
  - production local `origin/main` ref was not fetched and still showed `af1a2c9`;
  - daemon pid remained `69476`;
  - boot_id remained `3d3f4cb6f082b54e`;
  - launchd runs remained `57`;
  - watchdog remained unloaded;
  - `PRAGMA quick_check` returned `ok`;
  - `.harness/` and `scripts/harness/` remained absent;
  - no active hooks referenced `scripts/harness`, `agent_hook.py`, `pytest tests/`, or `python -m pytest tests/`;
  - no pytest/full-suite process was detected.
- Production still runs F1.2/F1.3 code at `af1a2c9`; F1.4 code is merged to remote main but not live.
- Next required separate authorization: deploy F1.4 code, smoke the live diagnostics/watchdog stale-event filter, then controlled watchdog load/re-enable only if smoke proves stale historical errors are non-actionable/unknown and current-window errors remain actionable.
- Still prohibited without separate authorization: deploy, daemon restart, launchd mutation, watchdog load/re-enable, full suite against live prod, `pytest tests/` from production checkout, F2/F3/F4/F5/F6, buddy.db mutation, RuntimeDb/WAL-heal/synchronous changes, and experimental harness reintroduction.
