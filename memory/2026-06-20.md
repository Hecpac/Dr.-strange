# 2026-06-20

## PR #124 merged, not deployed

- 2026-06-20 18:16:08 UTC: PR #124 "F1.2/F1.3 RuntimeDb WAL-heal retirement" was merged.
- Merge commit / new remote `origin/main`: `af1a2c954bf17b4b298ab0bc7f2b957a5f02567c`.
- PR head merged: `4042aa18983f2f64f9283c5689af90f51f05e47a`.
- Production was not deployed or fast-forwarded. The production checkout remains at `c232c07e1c91e1923fa7e0a2d65c4f3dfb4ae4c1`.
- The running production daemon is still pid `15601`, boot_id `3961df3b04e58224`, startup marker/code_version `c232c07`, launchd runs `56`.
- F1.2/F1.3 code is merged but not live. Deploy requires separate explicit authorization.
- Watchdog remains unloaded until F1.4.
- WAL-heal is still present in the running daemon until F1.2/F1.3 deploy.
- Full suite / `pytest tests/` against live production remains prohibited.
- No deploy, daemon restart, launchd mutation, watchdog load, full-suite run, buddy.db touch, or harness reintroduction was performed during the merge.

## F1.2/F1.3 deployed

- 2026-06-20 18:24:40 UTC: F1.2/F1.3 was deployed by fast-forwarding production to `af1a2c954bf17b4b298ab0bc7f2b957a5f02567c` and restarting only with `launchctl kickstart -k gui/$UID/com.pachano.claw`.
- Pre-deploy known-good commit was `c232c07e1c91e1923fa7e0a2d65c4f3dfb4ae4c1`.
- Pre-deploy DB backup: `data/claw.db.bak-pre-f1-2-f1-3-deploy-20260620-182415`; backup `PRAGMA quick_check` was `ok`.
- Old daemon: pid `15601`, boot_id `3961df3b04e58224`, launchd runs `56`.
- New daemon: pid `69476`, boot_id `3d3f4cb6f082b54e`, startup marker/code_version `af1a2c9`, launchd runs `57`.
- Immediate and 15-minute smoke were green: diagnostics `healthy`, `heartbeat_stale=false`, `web_transport_serving=true`, diagnostics heartbeat source `liveness_sink`, `quick_check=ok`, `integrity_check=ok`.
- Post-deploy observation window through 2026-06-20 18:43:30 UTC: pid/boot_id/runs stable; post-deploy `database is locked`, WAL/degraded DB, and daemon lifecycle error counters remained `0`.
- RuntimeDb/WAL-heal smoke: RuntimeDb production path no longer registers WAL-heal; RuntimeDb-backed stores only keep legacy heal in `runtime_db=None` backcompat path; observe maintenance VACUUM dedicated connection remains allowlisted.
- SQLite synchronous remains `FULL`; no `synchronous=NORMAL` carve-out is present.
- `property_graph` remains dormant in the production main path.
- `buddy.db` remains out of PR/code scope. No manual command was run against `buddy.db`; any touch during the window would be normal daemon operation after restart, not an F1.2/F1.3 migration step.
- Watchdog remains unloaded until F1.4.
- No full suite / `pytest tests/` was run from production.
- Next gate: post-F1.2/F1.3 observation before F1.4. Do not claim permanent cure from this smoke alone.
