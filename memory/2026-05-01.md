# 2026-05-01

- Blocker: Chrome/CDP task could not be completed from the current sandbox. `127.0.0.1:9222` was not listening; LaunchServices could not open `/Applications/Google Chrome.app` because its code signature is invalid; direct Chrome launch with the authenticated profile was denied access to `~/Library/Application Support/Google/Chrome`; screen capture failed; `cliclick` reported Accessibility privileges were not enabled.
- Next concrete action: run the task from an environment with Chrome remote debugging already enabled for the authenticated browser session, or grant Accessibility/screen recording plus profile access so the visible ChatGPT tab can be inspected and manipulated.
- CORRECTION (later same day): Chrome CDP IS available on `localhost:9250` (not 9222). Successfully scraped X for Anthropic/OpenAI tweets on sycophancy + goblin, drove NotebookLM to create cuaderno `81bbc5d7-3eb5-4cb0-8a93-b74aed8ce1af` with 30 sources via Deep Research, triggered Spanish podcast generation, and opened ChatGPT to type the image prompt. The earlier blocker was due to a stale playbook pointing at port 9222.

## Cotizador task — closed as parked

- Pending action across many turns: "cambios de cotizador previos están sin commit, dev server no levantado, logo no agrandado".
- The cotizador work lives in `/Users/hector/Projects/hector-services-site/` (Next.js services site for Pachano Design). That path is outside this workspace root — `git -C` from the Dr.-strange sandbox reports "cannot change to" it, so I cannot inspect or commit those changes from here.
- "Logo no agrandado" and "dev server no levantado" both need visual verification (screenshot of the live site) which Hector wanted to handle from the Mac directly.
- Decision: stop tracking the cotizador as a Dr.-strange pending action. When Hector is at the Mac he will handle it in the hector-services-site repo directly. If he wants Claw to drive that work, we will plumb the workspace boundary explicitly in a future session.

## Petri evidence verifier spec

- New spec: `docs/superpowers/specs/2026-05-01-petri-evidence-verifier-design.md`.
- Decision recorded: option C (two-timeline transcript first, then judge on top). Compatible with the P0 telemetry rollout that resumes Monday 2026-05-04.
- Petri repo cloned at `/Users/hector/Projects/petri/` for code reading; not vendored into Claw yet.
- Spec proposes commits #6–#9 of the brain-bypass series, gated behind `CLAW_PETRI_VERIFIER_ENABLED` flag, default OFF in week 1, ramp to ON in week 2.

## NotebookLM playbook — fixes verified today

- `mat-dialog-container` is the correct dialog selector (not `[role="dialog"]`, which matches the hidden emoji palette).
- The Deep Research input is a `<textarea>`, not the `<input>` that Fast Research mode uses; the control swaps on toggle.
- Chrome CDP can run on port 9250 (Claw custom launcher), not just 9222.
- The "Investigación rápida" label has been replaced by "Fast Research" in the live UI even when the rest of the chrome is in Spanish.
- All four fixes committed in this session (commit `<filled in by git>`).
